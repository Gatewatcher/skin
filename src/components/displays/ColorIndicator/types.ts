import type {
  COLOR_INDICATOR_COLORS,
  COLOR_INDICATOR_SIZES,
} from './constants';

export type ColorIndicatorSize = typeof COLOR_INDICATOR_SIZES[number];
export type ColorIndicatorColor = typeof COLOR_INDICATOR_COLORS[number];
