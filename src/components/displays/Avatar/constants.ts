import type { AvatarSize } from './types';

export const AVATAR_SIZES = ['small', 'medium', 'large', 'xLarge'] as const;

export const DEFAULT_SIZE: AvatarSize = 'medium';
export const DEFAULT_WITH_TOOLTIP = true;

export const DEFAULT_STACK_MAX = 3;
