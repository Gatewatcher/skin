import type { Mode } from '@gatewatcher/bistoury/utils-date';
import type { Mode as DateMode } from '@gatewatcher/bistoury/utils-date';

export const DEFAULT_DATE_MODE: Mode = 'absolute';
export const DATE_MODES: DateMode[] = ['absolute', 'relative'];
export const DATE_LONG_FORMAT = 'L HH:mm:ss';
export const DEFAULT_LOCALE = 'en-gb';
