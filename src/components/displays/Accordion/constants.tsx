import type { TestId } from '@gatewatcher/bistoury/utils-types';

export const DEFAULT_DEFAULT_EXPANDED = false;
export const TEST_ID: TestId = 'accordion';
export const SUFFIX_TEST_IDS = ['title', 'body'] as const;
