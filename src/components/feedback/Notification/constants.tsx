import type { Mode as DateMode } from '@gatewatcher/bistoury/utils-date';

import { RISK_TYPES } from '@/constants';

export const DEFAULT_DATE_MODE: DateMode = 'relative';

export const NOTIFICATION_TYPES = RISK_TYPES;

export const DEFAULT_WITH_DIVIDER = false;
